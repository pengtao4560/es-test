server:
  port: 8118


spring:
  application:
    name: JWT

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # ?????????
    driver-class-name: com.mysql.jdbc.Driver              # mysql???
    url: jdbc:mysql://localhost:3306/jwt?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
